# Base URL of the FastAPI backend, no trailing slash.
# The live backend in this project mounts all routers directly on the app
# (auth/resume/interview routers are NOT under /api/v1 - verified against
# app/main.py). Do not append /api/v1 here.
VITE_API_BASE_URL=http://127.0.0.1:8000
